# Rust build output
/target/

# Private docs & working rules
CLAUDE.md
internal/
.claude/

# macOS
.DS_Store

# Local scratch
*.local.*

# Generated media from running speak/image in the repo root
/*.wav
/*.png

# Stray Swift-era output (pre-Rust, not part of this branch)
.build/
dist/
packaging/

# xcodebuild derivedData for the MLX shim (produces the Metal library)
/runtime/shim-mlx/.xcode-dd/
/runtime/shim-mlx/.swiftpm/
