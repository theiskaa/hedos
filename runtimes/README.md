# Runtimes

Built-in runtime definitions ship from here: one directory per runtime, holding its declarative manifest (what it can execute, how to detect matching models, what permissions it needs), its dependency lockfile, and — for Python runtimes — the sidecar entrypoint Hedos spawns.

Nothing lives here yet; the first runtime arrives with the TTS milestone.
